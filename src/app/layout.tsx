import type { Metadata } from "next";
import { Fraunces, IBM_Plex_Sans } from "next/font/google";
import "./globals.css";

const fraunces = Fraunces({
  variable: "--font-fraunces",
  subsets: ["latin"],
  weight: ["400", "500", "600"],
  style: ["normal", "italic"],
});

const plex = IBM_Plex_Sans({
  variable: "--font-plex",
  subsets: ["latin"],
  weight: ["400", "500", "600"],
});

export const metadata: Metadata = {
  title: "Samiullah Khan — English Teacher & Digital Skills",
  description:
    "Portfolio of Samiullah Khan: English Linguistics graduate, English teacher, and digital & office skills specialist covering IT support, MS Office, PowerPoint, content writing, SEO, and digital marketing.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${fraunces.variable} ${plex.variable} antialiased`}>
        {children}
      </body>
    </html>
  );
}
